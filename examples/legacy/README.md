# Ранний прототип GitActivity

`GitActivity/date.py` и `GitActivity/save_log.py` перенесены из `test/GitActivity`.
Они сохранены как примеры compatibility-обёрток; автоматических тестов здесь нет.
В новом коде используйте `GitCollector` и `GitActivityAnalyzer` из `sourcehealth.git`.
Все автоматические проверки проекта находятся в корневом каталоге `tests/`.
